@{
    ViewBag.Title = "WomensClothing";
}

<h2>WomensClothing</h2>

@using (Html.BeginForm("SearchList", "Clothing", FormMethod.Get)) {

    @Html.TextBox("query")
    <input type="submit" value="Search" />
}